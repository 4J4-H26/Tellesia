using UnityEngine;
using UnityEngine.UI;

public class questionLore : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    [SerializeField] public Image reponse1;
    [SerializeField] public Image reponse2;
    [SerializeField] public Image reponse3;

    [SerializeField] public AudioSource audiorep1;
    //[SerializeField] public AudioSource reponse2;
    //[SerializeField] public AudioSource reponse3;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
