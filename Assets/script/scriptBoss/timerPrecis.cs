using UnityEngine;

public class timerPrecis : MonoBehaviour
{
    public float tempsOriginal = 5f;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
