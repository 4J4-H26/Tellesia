//------------------------------------------*
// Auteur : David Phung
// Description : Script qui permet au joueur de quitter le jeu en cliquant sur un bouton 
//------------------------------------------*

using UnityEngine;

public class GestionQuitterJeu : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void FermerJeu()
    {
        Application.Quit();
    }
}
